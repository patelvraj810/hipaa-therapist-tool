import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'TheraSecure — HIPAA-Compliant Practice Management',
  description: 'Secure practice management for solo therapists. Encrypted patient records, scheduling, billing, and full HIPAA compliance for $49/month.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}